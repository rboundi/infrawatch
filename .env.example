# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=infrawatch
DB_USER=infrawatch
DB_PASSWORD=infrawatch_dev

# Server
PORT=3001
NODE_ENV=development

# Encryption key for stored credentials (generate a random 32+ char string)
MASTER_KEY=change-me-to-a-random-secret

# SMTP (for alert emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Alert recipient
ALERT_EMAIL=
